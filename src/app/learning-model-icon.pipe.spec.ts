import { LearningModelIconPipe } from './learning-model-icon.pipe';

describe('LearningModelIconPipe', () => {
  it('create an instance', () => {
    const pipe = new LearningModelIconPipe();
    expect(pipe).toBeTruthy();
  });
});
